# Raw data — too large for git
sales.csv
data/processed/

# Model checkpoints (binary, large) — share via Drive if teammates need them
models/*.pt

# Note: data/predictions/ is intentionally tracked so the ensemble
# integration can consume each model's *_test_predictions.csv from git.

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.test_tmp_preprocessing/
.tmp/

# Jupyter
.ipynb_checkpoints/

# Virtual envs
venv/
.venv/
env/

# OS / IDE
.DS_Store
Thumbs.db
